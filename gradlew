#!/bin/sh
# Minimal launcher placeholder. Android Studio can regenerate/download the Gradle wrapper.
echo "Please open this project in Android Studio or install Gradle 8.x, then run: gradle assembleDebug"
exit 1
